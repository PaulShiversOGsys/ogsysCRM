@model ogsysCRM.ViewModels.NoteViewModel

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    @Html.Partial("_Note", Model)
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Id }) |
    <a href="@Model.LastPage">Back</a>
</p>
