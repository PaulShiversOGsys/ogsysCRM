@model ogsysCRM.ViewModels.DetailsCustomerViewModel


@{
    ViewBag.Title = "Index";
}

<h2>Notes for @Model.FirstName @Model.LastName</h2>


@Html.Partial("_Notes", Model.Notes)

<p>
    @Html.ActionLink("Create New", "Create", new { customerId = Model.Id }) |
    @Html.ActionLink("Back", "Details", "Customer", new { id = Model.CustomerId }, null)
</p>
